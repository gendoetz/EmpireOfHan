r50q2 = {
on_death = function(player)
	--local item = player:getEquippedItem(EQ_WEAP)

	--if (item.protectnum == 0) then
	--player:deductDura(EQ_WEAP, 9999999)
	--elseif (item.protectnum > 0) then

end,
--equip = function(player)
	--local t = {graphic = convertGraphic(1, "monster"), color = 1}
	--player.dialogType = 0
	--player.npcGraphic = t.graphic
	--player.npcColor = t.color

	--local item = Item(player.equipID)

	--player:dialogSeq({t, "Protected # "..item.name}, 1)
--end
}